import{utmify}from"./utmify"; import type{CanonicalEvent}from"../types";
export function kiwify(input:any):CanonicalEvent { const customer=input.Customer??input.customer??{};const products=(input.Products??input.products??[]).map((x:any)=>({id:String(x.product_id??x.id??"product"),name:x.product_name??x.name??"Produto",priceInCents:Math.round(Number(x.price??x.price_in_cents??0)*(x.price_in_cents?1:100))})); const statuses:Record<string,string>={order_approved:"paid",order_rejected:"refused",order_refunded:"refunded",chargeback:"chargedback",pix_created:"waiting_payment",billet_created:"waiting_payment"}; return utmify({orderId:String(input.order_id??input.order_ref??"unknown"),platform:"Kiwify",paymentMethod:input.payment_method,status:statuses[String(input.webhook_event_type)]??"paid",createdAt:input.created_at??new Date().toISOString(),approvedDate:input.approved_date,customer:{name:customer.full_name??customer.name??"Cliente",email:customer.email??"no-email@local.invalid",phone:customer.mobile??null,document:customer.CPF??null},products,commission:{totalPriceInCents:Math.round(Number(input.Commissions?.charge_amount??input.total_price??0)*100)},isTest:Boolean(input.is_test)}); }
