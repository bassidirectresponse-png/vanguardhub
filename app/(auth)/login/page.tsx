import { signIn } from "@/lib/auth";
import { redirect } from "next/navigation";
export default async function LoginPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; async function login(formData: FormData) { "use server"; try { await signIn("credentials", { email: formData.get("email"), password: formData.get("password"), redirectTo: "/" }); } catch { redirect("/login?error=1"); } } return <main className="grid min-h-screen place-items-center bg-[#090b10] p-5"><section className="panel w-full max-w-md p-8 shadow-2xl"><div className="mb-8 flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#87f0bf] text-xl font-black text-[#082017]">V</span><div><h1 className="font-bold">Vanguard Hub</h1><p className="text-sm text-[#9099aa]">Operações internas</p></div></div><h2 className="mb-2 text-2xl font-bold">Entrar</h2><p className="mb-6 text-sm text-[#9099aa]">Acesse seu painel de operações.</p>{error && <p className="mb-4 rounded-lg bg-[#49202a] p-3 text-sm text-[#ffb0ba]">Credenciais inválidas</p>}<form action={login} className="space-y-4"><label className="block text-sm font-medium">E-mail<input className="input mt-1.5" type="email" name="email" required autoComplete="email"/></label><label className="block text-sm font-medium">Senha<input className="input mt-1.5" type="password" name="password" required autoComplete="current-password"/></label><button className="btn btn-primary mt-2 w-full" type="submit">Entrar</button></form></section></main>; }
