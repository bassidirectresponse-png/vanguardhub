import{NextRequest,NextResponse}from"next/server";import{auth}from"@/lib/auth";import{deliverOutbox}from"@/lib/webhooks/outbox";import{z}from"zod";export async function POST(req:NextRequest){const s=await auth();if(s?.user.role!=="SUPER_ADMIN")return NextResponse.json({error:"forbidden"},{status:403});const p=z.object({id:z.string()}).safeParse(await req.json());if(!p.success)return NextResponse.json({error:"bad request"},{status:400});await deliverOutbox(p.data.id);return NextResponse.json({ok:true})}
