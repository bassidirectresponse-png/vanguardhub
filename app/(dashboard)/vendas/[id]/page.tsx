import { prisma } from "@/lib/prisma"; import { notFound } from "next/navigation"; import { dateTime,money } from "@/lib/utils"; import {StatusBadge} from "@/components/status-badge"; import Link from "next/link";
export default async function SaleDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const o=await prisma.order.findUnique({where:{id},include:{customer:true,items:{include:{product:true}},utm:true,accessLinks:true}});if(!o)notFound();return <div className="space-y-5"><div className="flex justify-between"><div><p className="text-sm text-[#87f0bf]">Pedido {o.externalOrderId}</p><h1 className="text-3xl font-bold">{money(o.amountCents)}</h1></div><StatusBadge status={o.status}/></div><div className="grid gap-5 lg:grid-cols-2"><section className="panel p-5"><h2 className="font-bold">Cliente</h2><p className="mt-3 font-medium">{o.customer.name}</p><p className="text-sm text-[#9099aa]">{o.customer.email}</p><Link className="mt-4 inline-block text-sm font-semibold text-[#87f0bf]" href={`/clientes/${o.customerId}`}>Abrir Cliente 360°</Link></section><section className="panel p-5"><h2 className="font-bold">Entrega</h2>{o.accessLinks[0]?.url?<a href={o.accessLinks[0].url} target="_blank" className="mt-3 block truncate text-sm text-[#87f0bf]">{o.accessLinks[0].url}</a>:<p className="mt-3 text-sm text-[#9099aa]">Sem link de entrega.</p>}</section></div><section className="panel p-5"><h2 className="font-bold">Itens</h2><div className="mt-3 space-y-2">{o.items.map(i=><div key={i.id} className="flex justify-between rounded-lg bg-[#171b23] p-3"><span>{i.product.name} <span className="text-xs text-[#9099aa]">{i.isUpsell?"Upsell":i.isOrderBump?"Order bump":i.product.kind}</span></span><span>{money(i.amountCents)}</span></div>)}</div></section><section className="panel p-5"><h2 className="font-bold">UTMs</h2><p className="mt-3 text-sm text-[#9099aa]">Fonte: {o.utm?.utmSource ?? "—"} · Campanha: {o.utm?.utmCampaign ?? "—"} · {dateTime(o.createdAt)}</p></section></div>}
